# OAuth folder replacement

Replace your existing `oauth` folder with this folder.

Final structure:

```text
oauth/
├── index.html
├── oauth2-app-service-aks-identity-reference/
│   └── index.html
└── oauth2-app-service-aks-identity-feynman/
    └── index.html
```

Then run:

```powershell
git add oauth
git commit -m "Organize OAuth identity study notes"
git push
```
